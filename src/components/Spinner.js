import React from 'react'

const Spinner = () => {
    return (
        <div className="spinner">
            <img src={require('../images/loading.gif')} alt="Spinner"/>
        </div>
    )
}

export default Spinner
